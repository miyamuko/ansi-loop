package: ansi-loop
version: 1.0.1

sources:
  include:
    - site-lisp/ansi-loop/**/*.l
  exclude:
    - site-lisp/ansi-loop/**/test/*.l

document:
  - README.md

depends:
  - ansify

files:
  include:
    - site-lisp/**/*.l
    - site-lisp/**/*.lc
